"use strict";
const expect = require("chai").expect;
const vueFetch = require("../dist/index.js");

describe("vueFetch function test", () => {
  it("should return Success", () => {
    let result = true;
    expect(result).to.equal(true);
  });
});
